name: Build
on:
  pull_request:
    paths:
      - 'Dockerfile'
      - '.dockerignore'

jobs:
  build:
    name: Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build image
        run: docker build . -t local

      - name: Test Python version
        run: docker run local python --version

      - name: Test CMake version
        run: docker run local cmake --version

      - name: Test ARM compiler version
        run: docker run local arm-none-eabi-gcc --version
